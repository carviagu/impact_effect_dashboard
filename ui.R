library(shiny) # librería de shiny

# interfaz grafica (frontend)
shinyUI(
  fluidPage(
    
    # Title of the app
    titlePanel("Impact Effect Dashboard"),
    
    # Main panel
    sidebarLayout(
      
      # Options section
      sidebarPanel(
        
      ),
      
      # Plots area
      mainPanel(
        
      )
      
    )
    
  )
) # UI

